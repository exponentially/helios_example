defmodule HeliosExampleTest do
  use ExUnit.Case
  doctest HeliosExample

  # test "greets the world" do
  #   id = UUID.uuid4()
  #   params = %{"id" => id, "first_name" => "Pera", "last_name" => "Detlic", "email" => "pera@detlic.com"}
  #   assert {:ok, :created} = HeliosExample.create_user(id, params)
  # end
end
